// Developer console intentionally disabled in production.
// Runtime diagnostics remain available through normal error/status reporting without
// wrapping console methods, mirroring every runtime event, or building a persistent
// diagnostic UI on startup.
export {};
