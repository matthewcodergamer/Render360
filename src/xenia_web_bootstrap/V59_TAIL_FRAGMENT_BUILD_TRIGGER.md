# Render360 V59 tail-fragment bootstrap trigger

This marker intentionally touches the browser-bootstrap source path so GitHub Actions rebuilds the published WASM after the V59 source was generated by the Apply Tail Fragment workflow.

The V59 runtime change preserves the working V58 shared-epilogue bridge and adds only the exact missing-interior-HIR tail-fragment fallback plus corrected unresolved-tail diagnostics.
