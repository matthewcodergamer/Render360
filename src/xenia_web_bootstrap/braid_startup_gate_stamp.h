#pragma once

// Build stamp: force the authoritative wasm32 gate to rebuild and publish a
// browser bootstrap containing the current sparse-memory, main-thread ABI, and
// XAM notification startup fixes.
#define RENDER360_BRAID_STARTUP_GATE_REVISION 1
